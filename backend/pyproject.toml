[project]
name = "aisearch-backend"
version = "0.1.0"
description = "通智 AI 搜索 - 后端(FastAPI + qwen3.8-flash 联网搜索/网页抓取)"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "openai>=1.50",
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "python-dotenv>=1.0",
    "apscheduler>=3.11.3",
    "langgraph>=1.2.11",
]

[tool.uv]
package = false
